spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shiro
    username: root
    password: ss300400
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
